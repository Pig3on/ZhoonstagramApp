import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderColor: 'white',
    borderWidth: 1,
    borderRadius: 10,
    color: 'white',
    padding: 10,
  },
  buttonText: {
    color: 'white',
    textAlign: 'center',
  },
});

export default styles;
